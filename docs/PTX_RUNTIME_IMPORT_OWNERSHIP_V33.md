# PTX Runtime Import Ownership v33

After authorized copying from read-only `dmc-rengine-cpp@50d070e...`, the copied implementation becomes a **Native Reader local compatibility layer** maintained in `VrUaCom/DMC-Native-Reader` only.

The source evidence/provenance remains attributed to the reverse-recovered snapshot, but future code maintenance for the imported copy does not mutate Rengine.

This local ownership does not make Native Reader the canonical authority for the original game's reverse research. It means Native Reader owns its product-side C++23 compatibility implementation of the imported confirmed behavior.
