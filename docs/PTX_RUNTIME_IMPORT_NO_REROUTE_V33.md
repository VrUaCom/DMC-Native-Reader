# PTX Runtime Import — No Reroute to Rengine v33

If implementation discovers missing behavior, do not redirect work into `dmc-rengine-cpp`. Record the missing evidence as a blocker/deferred item in Native Reader Project. Continue only with behavior supported by the authorized read-only source snapshot or a later owner-authorized Reader-side evidence import.
