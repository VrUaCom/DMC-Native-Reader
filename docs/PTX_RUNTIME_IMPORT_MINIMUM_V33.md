# PTX Runtime Import Minimum v33

Minimum accepted functional slice for first integration:
- initialize pool/runtime state;
- configure reservation;
- place/validate record;
- reset confirmed manager key/count state;
- preserve confirmed pool occupancy/records/payload semantics;
- expose typed inspection state.

Everything else remains out of scope until separately reviewed.
