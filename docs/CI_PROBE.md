# CI Probe

This file exists only to trigger and prove the standalone pull-request GitHub Actions path for the canonical DMC-Native-Reader repository.

Acceptance: opening/updating a PR against `main` must start `.github/workflows/android.yml` and produce the verified APK artifacts.
