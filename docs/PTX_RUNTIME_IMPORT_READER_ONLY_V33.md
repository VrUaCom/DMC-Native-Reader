# PTX Runtime Import — Reader Only v33

All copied/adapted runtime source is created and maintained only in `VrUaCom/DMC-Native-Reader`.

The external reverse repository is never modified. Reading it for the authorized PTX source snapshot is allowed; all writes remain in Native Reader.
